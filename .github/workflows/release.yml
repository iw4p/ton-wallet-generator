name: Build and Release

on:
    push:
        tags:
            - "v*"

permissions:
    contents: write

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Go
              uses: actions/setup-go@v4
              with:
                  go-version: "1.21"

            - name: Build for Linux
              run: |
                  GOOS=linux GOARCH=amd64 go build -o ton-wallet-manager-linux-amd64 ./cmd/ton-wallet-manager
                  GOOS=linux GOARCH=arm64 go build -o ton-wallet-manager-linux-arm64 ./cmd/ton-wallet-manager

            - name: Build for macOS
              run: |
                  GOOS=darwin GOARCH=amd64 go build -o ton-wallet-manager-darwin-amd64 ./cmd/ton-wallet-manager
                  GOOS=darwin GOARCH=arm64 go build -o ton-wallet-manager-darwin-arm64 ./cmd/ton-wallet-manager

            - name: Build for Windows
              run: |
                  GOOS=windows GOARCH=amd64 go build -o ton-wallet-manager-windows-amd64.exe ./cmd/ton-wallet-manager
                  GOOS=windows GOARCH=arm64 go build -o ton-wallet-manager-windows-arm64.exe ./cmd/ton-wallet-manager

            - name: Create Release
              uses: softprops/action-gh-release@v1
              with:
                  files: |
                      ton-wallet-manager-linux-amd64
                      ton-wallet-manager-linux-arm64
                      ton-wallet-manager-darwin-amd64
                      ton-wallet-manager-darwin-arm64
                      ton-wallet-manager-windows-amd64.exe
                      ton-wallet-manager-windows-arm64.exe
                  generate_release_notes: true
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
